---
name: chaoyun-ppt-design
description: 超云PPT设计：将文章、讲稿、Markdown、TXT、DOCX 或可提取文字的 PDF 转换为高信息密度、整页文生图的视觉 PPTX。先建立内容覆盖大纲，再从中文风格目录中锁定整套风格，生成逐页提示词和完整页面图片，最后组装并质检。适用于从文章新建演示文稿；不用于修改现有 PPTX，也不用于用户明确指定的固定国潮课程风格。
---

# 超云PPT设计

制作一套来源清楚、信息完整、风格统一的演示文稿。默认让 Image2 直接生成包含背景、图片、标题和正文的完整 PPT 页面图片，而不是把图片与文字分开排版。只有表格、精确数据、日期、价格或长文本无法可靠生成时，才允许改用原生 PowerPoint 对象兜底。

## 适用范围

- 用户明确指定固定的暖宣纸、绿色山水国潮课程风格时，改用 `article-to-guochao-course-ppt`。
- 用户要修改现有 PPTX 时，改用演示文稿编辑流程。
- 扫描版 PDF 无法提取文字时，停止并说明需要文字稿或先做文字识别。
- 保留原始文件和提取文本，不编造事实、案例、数据、引语或作者观点。
- 生成的图片不是证据；每条重要结论必须能回到原文锚点。

## 必读资料

按当前阶段读取对应资料，不必一次全部加载：

1. 规划页面前，读取 [内容规划](references/content-planning.md)。
2. 选择或确认视觉方向前，读取 [视觉系统](references/visual-system.md)；需要挑选风格时，再读取 [中文风格目录](references/style-catalog.md)。
3. 编写逐页提示词或生成图片前，读取 [提示词组装](references/prompt-assembly.md)。
4. 编写构建清单或交付前，读取 [构建清单规范](references/manifest-schema.md) 和 [交付与质检](references/delivery-and-qa.md)。

## 执行模式

- `guided`（把关模式）为默认模式：在下方三个轻量把关点暂停。
- `auto`（全自动模式）：当用户明确要求“不用确认、直接做完、全自动生成”时启用。仍需在内部完成大纲、覆盖矩阵、风格候选和代表页，但不暂停询问；自行选择最合适的方案并记录理由，最后一次性交付。

全自动模式不会降低来源约束、信息密度、视觉质检、重试次数或交付标准。只有遇到无法安全解决的硬阻塞才停止，例如来源无法读取、事实严重冲突、缺少必要权限，或连续失败后没有可用页面和替代方案。

### 模式切换

- `guided → auto`：保留已确认内容，跳过后续把关点，直接做到交付；不得重做已经通过的阶段。
- `auto → guided`：完成当前正在生成或导出的单个动作，在下一个安全决策点暂停，并展示与该决策有关的最新大纲、风格样张或质检结果。
- 用户可以只把某一阶段设为自动，例如大纲人工确认、风格自动选择、批量生成前再次暂停。

模式切换不能删除或替换已经确认的成果。只有用户明确要求回到前一步时才允许重做，并在决策记录中写明保留内容和下一检查点。

## 工作流程

### 1. 整理并映射原文

提取原文并保存工作副本。识别受众、沟通目标、核心结论、论据、案例、方法、边界和行动建议。先建立稳定的内容单元编号和覆盖矩阵，不按段落机械分页。

### 2. 规划内容覆盖大纲

根据内容单元的数量和复杂度决定页数，不强行固定。一般文章可参考 10—24 页，但原文覆盖和用户要求优先。

每页必须只有一个叙事任务，并记录：

- 页码与页面类型
- 面向观众的准确标题
- 本页核心结论
- 页面可见文字
- 原文锚点和覆盖的内容单元编号
- 视觉构想与页面生成模式

把关模式在这里展示精简大纲和覆盖摘要，这是把关点一。全自动模式只有在核心结论、核心论据、重要方法和关键边界都有明确去向，且没有新增无来源内容后，才能内部通过。

### 3. 比较并确认整套风格

推荐模板前，按 [视觉系统](references/visual-system.md) 的全库检索流程读取 [全库索引](references/style-index.json)。索引使用飞书当前名称、标签、PPT主要使用场景（单选）、推荐使用场景（多选）和建议正文容量；结合擅长的内容结构、页面组织方式、素材承载方式和判断依据匹配文章；不得只推荐目录前几项。

风格来源按以下优先级选择：

1. 用户提供的参考 PPT 或图片
2. 用户明确指定的视觉方向
3. [中文风格目录](references/style-catalog.md)中的风格；需要查看真实案例时，读取对应编号的 [PPT 预览图](assets/ppt-previews/)

中文风格目录来自用户指定的[飞书多维表格](https://xcn5phmhnu1z.feishu.cn/base/JvecbWMEwaLv8as3NGXcQxjgnn9?table=tbl1DwIeLHneaDRX&view=vew2lCosPH)，它是风格名称、描述、场景和完整提示词的唯一权威来源。`assets/ppt-previews/` 保存与目录编号一一对应的 207 张案例预览图，用于理解真实视觉效果；预览图不能覆盖表格中的文字定义。不得再从旧英文或日文案例库提取风格名称、描述或提示词。

用户没有锁定唯一参考时，提出 2—3 个差异明显且适合原文的中文风格方向。用同一张有代表性的内容页分别生成样张，再对比语气、配色、图像语言、信息密度适配度和风险。每个方向单独出图，避免风格互相污染。

把关模式在这里请用户选择一个方向或提出一次针对性调整，这是把关点二。用户给了唯一参考时，只生成一张适配样张。全自动模式按内容适配、可读性、一致性和受众匹配程度排序，自行选出最优风格并记录理由。

### 4. 锁定视觉系统并编写全部提示词

建立统一视觉规范，包括画面比例、配色、材质、插画或摄影语言、字体气质、留白、重复视觉母题、文字安全区和禁止项。

批量生图前必须先编写完全部逐页提示词。每页提示词由以下部分组成：

`风格锁定 + 页面任务 + 准确文案 + 构图要求 + 输出要求 + 禁止项`

页面模式只有三种：

- `full-image`：完整整页文生图，标题、正文、图形和背景直接生成在同一张页面图片中；这是默认模式。
- `hybrid`：Image2 只生成背景或插画，再叠加原生 PowerPoint 文字；仅在整页文字生成不可靠时使用。
- `native-data`：图表、表格、时间线、日期、价格、引文和精确数字使用原生 PowerPoint 对象，Image2 只提供辅助视觉。

信息太多时先拆页，不得通过缩小字号硬塞。

### 5. 生成正式代表页

选择一张内容丰富的正文页，而不是封面。代表页必须同时检验标题层级、正文、插画风格和留白。

风格候选样张解决“选哪个方向”，正式代表页解决“该方向在真实信息密度下是否可用”。把关模式在这里做最后一次确认，这是把关点三。全自动模式自行检查和修正，只有达到信息密度、可读性、来源和风格标准后才能锁定。

通过后保存为 `style-master.png`，后续每一页都以它作为视觉参考。此后不再逐页询问。

### 6. 批量生成、检查和修复

按页码生成稳定文件名，失败页只替换对应文件。每页检查：

- 文字是否错误、遗漏、多余或不可读
- 核心结论和论据是否完整
- 是否偏离原文或新增无来源内容
- 比例、裁切和文字安全区是否正确
- 是否偏离 `style-master.png`
- 是否出现无关人物、商标、水印、品牌或敏感信息
- 是否重复使用同一种构图导致整套 PPT 单调

失败页最多重试两次，每次只修一个明确问题。关键文字仍不可靠时，改用 `hybrid` 或 `native-data`，不要无限重生。

### 7. 组装并验证 PPTX

遵循本地 `Presentations` Skill 和运行环境要求。使用 `@oai/artifact-tool`，不得使用 `python-pptx`。

按 [构建清单规范](references/manifest-schema.md)编写清单。完整图片页和简单混合页使用 [scripts/build_ppt.mjs](scripts/build_ppt.mjs)；复杂图表、表格和原生布局直接用演示文稿工具制作，但必须保持同一视觉规范。

第一次创建演示文稿前，按 `Presentations` Skill 要求执行一次操作标记。最终逐页渲染并放大检查，再检查整套缩略图、溢出问题和交付文件。

## 交互规则

当用户询问“有多少种风格”“有哪些模板”“查看风格库”或相近问题时，除非用户明确要求分析或筛选推荐，否则只返回下面这个飞书链接：

`https://xcn5phmhnu1z.feishu.cn/base/JvecbWMEwaLv8as3NGXcQxjgnn9?table=tbl1DwIeLHneaDRX&view=vew2lCosPH`

把关模式只设置三个把关点：

1. 大纲和内容覆盖确认
2. 2—3 张风格样张的比较与选择；已有唯一参考时只确认一张适配样张
3. 一张正式正文代表页确认

不得增加风格问卷、逐页确认或日常微小确认，除非用户主动要求。其余合理默认值从原文和受众中推断。

全自动模式跳过三个暂停点，但必须把内部决定写入 `自动决策记录.md`。状态更新不算审批点。

## 交付内容

- 最终 `.pptx`
- 按页码排列的完整页面图片或背景图
- 整套缩略图
- 使用风格比较时的候选样张总览
- `逐页大纲.md`
- `内容覆盖矩阵.md`
- `Image2完整提示词.md`
- 需要或用户要求时提供 `每页PPT逐字稿.md`
- `质检记录.txt`
- 全自动模式提供 `自动决策记录.md`

不得把 `full-image` 页面说成完全可编辑。交付时说明哪些页面保留了原生可编辑文字或数据对象。
